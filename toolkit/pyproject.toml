[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "connectome_embedding"
version = "0.1.0"
description = "My connectome embedding project"
dependencies = [
    # "numpy", "torch", etc. (optional, just good practice)
]